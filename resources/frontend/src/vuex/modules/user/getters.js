const user = state => {
    return state.user
}

const role = state => {
    return state.role
}

const roles = state => {
    return state.roles
}

const company = state => {
    return state.company
}

export default {
    user,
    role,
    roles,
    company
}
